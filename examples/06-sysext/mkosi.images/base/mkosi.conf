# Base Ubuntu image for sysext overlay

[Distribution]
Distribution=ubuntu
Release=noble

[Output]
Format=directory

[Content]
# Keep package metadata so the sysext can install packages on top
CleanPackageMetadata=no
Packages=
        systemd
        udev
        dbus
        iproute2
        ca-certificates
