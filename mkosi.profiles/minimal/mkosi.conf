# Minimal Ubuntu Server Profile
#
# A stripped-down server image with just the essentials.
# Build with: mkosi --profile minimal

[Output]
Format=directory

[Content]
Packages=
        openssh-server
        openssh-client
